package com.example.campusmate.app

/** Static app-level constants shared across layers. */
object AppConfig {
    const val APP_NAME = "CampusMate"
    const val PACKAGE_NAME = "com.example.campusmate"
    const val PROVIDER_AUTHORITY = "$PACKAGE_NAME.provider"
}
